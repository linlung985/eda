---
- name: Prometheus Alertmanager
  hosts: all
  sources:
  - name: alertmanager
    ansible.eda.alertmanager:
      host: 0.0.0.0
      port: 5000
  rules:
  - name: Configuration Changed
    condition: event.alert.labels.alertname == "HighFilesystemUsage" and event.alert.status == "firing" and event.alert.labels.instance == "rhel8.workshop.lab:9100"
    action:
      run_job_template:
        name: email
        organization: Default
