---
- name: Hello Events  
  hosts: localhost
  sources:
  - name: kafka
    ansible.eda.kafka:
      host: 10.9.79.143
      port: 9092
      topic: eda
  rules:
  - name: Send email
    condition: event.payload.message == "eda"
    action:
      run_job_template:
        name: email
        organization: Default
